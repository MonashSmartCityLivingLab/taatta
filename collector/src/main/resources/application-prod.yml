spring:
  application:
    name: collector
  zipkin:
    base-url: http://localhost:9411/

server:
  port: 3030

logging:
  config: classpath:logback-prod.xml

smart:
  city:
    broker:
      url: tcp://${MOSQUITTO_HOST}:1883

